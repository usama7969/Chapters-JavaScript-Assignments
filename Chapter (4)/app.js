// 1
var a = "usama", b = "saleem", c = "smit";

// 2
// Legal Variable Names

// firstName
// _counter
// $price
// userAge
// totalAmount

// Illegal Variable Names

// 2ndPlace
// my-variable
// class
// @home
// var


document.write('<h1>Rules for naming JS variables</h1>');
        document.write('<p>Variable names can only contain letters, numbers, underscores (_), and dollar signs ($). For example: <code>Smy_1stVariable</code></p>');
        document.write('<p>Variables must begin with a letter, an underscore (_), or a dollar sign ($). For example: <code>Sname</code>, <code>_name</code>, or <code>$name</code></p>');
        document.write('<p>Variable names are case sensitive.</p>');
        document.write('<p>Variable names should not be JS keywords.</p>');

